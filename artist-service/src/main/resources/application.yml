server:
  port: 8082

spring:
  application:
    name: artist-service
  datasource:
    url: jdbc:h2:mem:artists;DB_CLOSE_DELAY=-1;MODE=PostgreSQL
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
    open-in-view: false
  flyway:
    enabled: false

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

management:
  endpoints:
    web:
      exposure:
        include: health,info

riffrank:
  admin-key: dev-admin-key

