server:
  port: 8083

spring:
  application:
    name: web-ui
  thymeleaf:
    cache: false

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

management:
  endpoints:
    web:
      exposure:
        include: health,info

riffrank:
  gateway-base-url: http://localhost:8080

